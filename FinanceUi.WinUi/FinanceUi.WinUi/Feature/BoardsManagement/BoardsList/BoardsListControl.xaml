<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="FinanceUi.WinUi.Feature.BoardsManagement.BoardsList.BoardsListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:board="using:FinanceUi.Core.Dtos.Board"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FinanceUi.WinUi.Feature.BoardsManagement.BoardsList"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance local:BoardsListViewModel}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="SubtitlePanel" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal" />
            <Setter Property="Spacing" Value="4" />
        </Style>
        <Style x:Key="Subtitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Light" />
        </Style>
    </UserControl.Resources>

    <ListView ItemsSource="{Binding Boards}">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="board:BoardDto">
                <StackPanel>
                    <TextBlock Text="{x:Bind Title}" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <StackPanel Style="{StaticResource SubtitlePanel}">
                            <TextBlock Style="{StaticResource Subtitle}">Доходов:</TextBlock>
                            <TextBlock Style="{StaticResource Subtitle}" Text="{x:Bind IncomesCount}" />
                        </StackPanel>
                        <StackPanel Style="{StaticResource SubtitlePanel}">
                            <TextBlock Style="{StaticResource Subtitle}">Расходов:</TextBlock>
                            <TextBlock Style="{StaticResource Subtitle}" Text="{x:Bind PaymentsCount}" />
                        </StackPanel>
                        <StackPanel Style="{StaticResource SubtitlePanel}">
                            <TextBlock Style="{StaticResource Subtitle}">Целей:</TextBlock>
                            <TextBlock Style="{StaticResource Subtitle}" Text="{x:Bind GoalsCount}" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
