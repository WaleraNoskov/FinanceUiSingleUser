<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FinanceUi.Windows.Feature.BoardsManagement.Controls.BoardsSearchBar.BoardsSearchBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FinanceUi.Windows.Feature.BoardsManagement.Controls.BoardsSearchBar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance local:BoardsSearchBarViewModel}"
    DataContextChanged="UserControl_DataContextChanged">

    <StackPanel Orientation="Horizontal" Spacing="4">

        <TextBox PlaceholderText="Search for your boards" Height="1"
                 Text="{Binding GetAllBoardsDto.Filter}" TextChanged="OnSearchTextChanged"
                 IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"/>
        
        <Button Style="{StaticResource AccentButtonStyle}" Content="Search"
                Command="{Binding RestoreCommand}"/>
    </StackPanel>
</UserControl>
